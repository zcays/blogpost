# blogpost

A simple blog-post-style website.

## Run locally

Open `/home/runner/work/blogpost/blogpost/index.html` in a browser, or serve it:

```bash
cd /home/runner/work/blogpost/blogpost
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
